apiVersion: v1
kind: PersistentVolume
metadata:
  name: datastorage
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  nfs:
    server: "10.36.9.147"
    path: "/"

---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: datastorage
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: 10Gi
